# BitsGPT Python dependencies
# pip install -r requirements.txt

# LLM — Groq (free tier: 14,400 req/day, 30k tokens/min)
groq>=0.11.0

# TF-IDF retrieval
scikit-learn>=1.3.0
numpy>=1.24.0

# PDF ingestion (optional — for adding extra source documents)
pypdf>=4.0.0

# API server
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Env file support
python-dotenv>=1.0.0
